# --- Toolchain pins ---
android.ndkVersion=27.1.12297006
cmake.dir=/data/data/com.termux/files/home/opt/android-sdk/cmake/3.30.5

# Use JDK 17 for broad AGP 8.1 compatibility on Termux
org.gradle.java.home=/data/data/com.termux/files/usr/lib/jvm/java-17-openjdk

# --- Gradle perf ---
org.gradle.caching=false
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.daemon=true

# --- AndroidX flags ---
android.useAndroidX=true
android.enableJetifier=false

# --- Force SDK aapt2 (arm64) instead of the maven one ---
android.aapt2FromMaven=false
android.enableAapt2Daemon=false
android.aapt2FromMavenOverride.enable=true
android.aapt2FromMavenOverride.path=/data/data/com.termux/files/home/opt/android-sdk/build-tools/34.0.4/aapt2

# (Optional) release keystore — if these exist, release will use them; otherwise falls back to debug keystore.
# RELEASE_STORE_FILE=/path/to/your.jks
# RELEASE_STORE_PASSWORD=...
# RELEASE_KEY_ALIAS=...
# RELEASE_KEY_PASSWORD=...
